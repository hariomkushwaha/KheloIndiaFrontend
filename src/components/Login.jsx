import { flexbox } from '@chakra-ui/system'
import React from 'react'

const Logout = () => {
  return (
    <div style={{display:'flex',justifyContent:'center',alignItems:'center',fontSize:'3em',height:'80vh',fontWeight:'bold'}}>
        Logged In!
    </div>
  )
}

export default Logout